# Backend API base URL (must include /v1)
# Local development (Express on port 4000)
VITE_API_URL=http://localhost:4000/v1

# Production (Cloudflare Worker — replace with your deployed URL)
# VITE_API_URL=https://your-worker.workers.dev/v1
